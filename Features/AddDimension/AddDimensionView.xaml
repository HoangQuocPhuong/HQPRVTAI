<Window x:Class="HQPRVTAI.Features.AddDimension.AddDimensionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"       
        Title="Add Aligned Dimension"
        Width="400" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- ── Màu sắc ── -->
        <SolidColorBrush x:Key="AccentBrush"   Color="#2962FF"/>
        <SolidColorBrush x:Key="DangerBrush"   Color="#E53935"/>
        <SolidColorBrush x:Key="SurfaceBrush"  Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderBrush"   Color="#E0E0E0"/>
        <SolidColorBrush x:Key="TextPrimary"   Color="#212121"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#757575"/>

        <!-- ── Button chính ── -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background"   Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground"   Value="White"/>
            <Setter Property="FontSize"     Value="13"/>
            <Setter Property="FontWeight"   Value="SemiBold"/>
            <Setter Property="Padding"      Value="24,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"       Value="Hand"/>
        </Style>
    </Window.Resources>

    <StackPanel Margin="24">
        <!-- Header -->
        <TextBlock Text="Add Aligned Dimension"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Foreground="#212121"
                   Margin="0,0,0,8"/>

        <!-- Instructions -->
        <TextBlock Text="Click the button below to add an aligned dimension between two selected faces or edges."
                   FontSize="12"
                   Foreground="#757575"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12"/>

        <!-- Button -->
        <Button Content="Add Dimension"
                Style="{StaticResource PrimaryButton}"
                Command="{Binding AddDimensionCommand}"
                Height="40"/>
    </StackPanel>
</Window>
